import { describe, expect, it, vi } from "vitest"; import { createAdminClientAccessSendHandler } from "@/app/api/admin/appointments/[id]/client-access/send/handler"; import type { RateLimitPolicy, RateLimitResult } from "@/lib/server/distributed-rate-limit";
const allowed: RateLimitResult = { allowed: true, retryAfterSeconds: 60 }; const context = { userId: "admin-1", email: "a@example.com", organizationId: "org-1", role: "admin" as const }; const appointment = { id: "appointment-1", organizationId: "org-1" };
function handler(overrides: Record<string, unknown> = {}) { const contextLookup = vi.fn().mockResolvedValue(context); const getAppointment = vi.fn().mockResolvedValue(appointment); const consume = vi.fn<(policy: RateLimitPolicy, identity: string) => Promise<RateLimitResult>>().mockResolvedValue(allowed); const send = vi.fn().mockResolvedValue({ record: { identifier: "safe" }, delivery: { status: "sent" } }); return { contextLookup, getAppointment, consume, send, handle: createAdminClientAccessSendHandler({ context: contextLookup, getAppointment, consume, send, ...overrides }) }; } const params = () => ({ params: Promise.resolve({ id: "appointment-1" }) });
describe("admin Client Workspace access-send rate limit", () => { it("orders context, ownership, limit, then send workflow", async () => { const target = handler(); expect((await target.handle(new Request("http://localhost"), params())).status).toBe(200); expect(target.contextLookup.mock.invocationCallOrder[0]).toBeLessThan(target.getAppointment.mock.invocationCallOrder[0]!); expect(target.getAppointment.mock.invocationCallOrder[0]).toBeLessThan(target.consume.mock.invocationCallOrder[0]!); expect(target.consume.mock.invocationCallOrder[0]).toBeLessThan(target.send.mock.invocationCallOrder[0]!); }); it("rejects wrong tenants before limiting and blocks/fails closed before sending", async () => { const wrong = handler({ getAppointment: vi.fn().mockResolvedValue({ ...appointment, organizationId: "other" }) }); expect((await wrong.handle(new Request("http://localhost"), params())).status).toBe(404); expect(wrong.consume).not.toHaveBeenCalled(); const blocked = handler({ consume: vi.fn().mockResolvedValue({ allowed: false, retryAfterSeconds: 8 }) }); const response = await blocked.handle(new Request("http://localhost"), params()); expect(response.status).toBe(429); expect(response.headers.get("Retry-After")).toBe("8"); expect(response.headers.get("Cache-Control")).toBe("no-store"); expect(await response.json()).toEqual({ status: "rate_limited" }); expect(blocked.send).not.toHaveBeenCalled(); const failed = handler({ consume: vi.fn().mockRejectedValue(new Error("store")) }); expect((await failed.handle(new Request("http://localhost"), params())).status).toBe(429); expect(failed.send).not.toHaveBeenCalled(); }); });
